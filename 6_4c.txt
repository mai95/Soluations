#include <stdio.h>
#include <stdlib.h>
#define Student 3
#define Exam 3
void printArray(const int array[][Exam], int row ,int coulum); // view arrays
int Counter(const int array[][Exam], int row, int coulum);     //sum of subscripts

int main(void)
{
   int arr[Student][Exam]= {0};
   int i, j;
   int counter= 0;
   int MM= Counter(arr, Student, Exam);

//loop to initialize array with the sum
   for(i= 0; i< Student; i++)
   {
       for (j= 0; j<Exam; j++)
       {
          arr[i][j]= MM;
       }
   }
   printArray(arr, Student, Exam); //view array

    return 0;
}

void printArray(const int array[][Exam], int row ,int coulum){
    int r,n ;
    printf("The initialized array is:\n");
    printf( "           [0] [1] [2]" );
    for(r= 0; r< row; r++)
   { printf("\nStudent[%i] ",r);
       for (n= 0; n<coulum; n++)
       {
          printf("%-3i ",array[r][n]);

       }
   }



}
int Counter(const int array[][Exam], int row, int coulum){
    int i,j;
    int counter=0;
    for(i= 0; i< row; i++)
   {
       for(j= 0;j< coulum; j++)
       {
           counter+= i+j;
       }

   }
   printf("The Sum of the subscripts is :\nSum= %i\n", counter);
   return counter;

}
